| #  | Topic                             | One-line Note                                                         |
| -- | --------------------------------- | --------------------------------------------------------------------- |
| 1  | `package main`                    | Marks the program as executable.                                      |
| 2  | `import`                          | Imports packages so you can use their functions.                      |
| 3  | `fmt` package                     | Used for input (`Scan`) and output (`Print`, `Println`).              |
| 4  | `math` package                    | Provides built-in mathematical functions like `Pow()`.                |
| 5  | `func main()`                     | The entry point where program execution begins.                       |
| 6  | Variables (`var`)                 | Declares variables with an explicit type.                             |
| 7  | Constants (`const`)               | Stores values that cannot be changed.                                 |
| 8  | Short Variable Declaration (`:=`) | Creates a new variable with inferred type.                            |
| 9  | Data Types                        | Learned `float64` for decimal numbers.                                |
| 10 | Zero Values                       | Variables get default values (`0`, `false`, `""`) if not initialized. |
| 11 | User Input                        | `fmt.Scan(&variable)` reads input from the user.                      |
| 12 | Ampersand (`&`)                   | Passes the variable's memory address so a function can modify it.     |
| 13 | Function Calls                    | Calling functions like `Print()`, `Scan()`, `Pow()`.                  |
| 14 | Package Functions                 | Access functions using dot notation (`fmt.Println`, `math.Pow`).      |
| 15 | Arithmetic Operators              | Used `+`, `-`, `*`, `/`.                                              |
| 16 | Expressions                       | Combining variables and operators to produce values.                  |
| 17 | Assignment (`=`)                  | Updates an existing variable.                                         |
| 18 | Variable Initialization           | Creating variables with or without an initial value.                  |
| 19 | Type Inference                    | Go automatically determines the type when using `:=`.                 |
| 20 | Program Flow                      | Execution happens from top to bottom inside `main()`.                 |
| 21 | :=                                | Creates a new variable and assigns a value.                           |
| 22 | Ex:var price float64              |  Every variable in Go gets a zero value automatically.                |
| 23 |fmt.Sprintf()                      |  Instead of printing, it returns a string.                            |
| 23 |                                   |                                                                       |
| 23 |                                   |                                                                       |
| 23 |                                   |                                                                       |
| 23 |                                   |                                                                       |
| 23 |                                   |                                                                       |
| 23 |                                   |                                                                       |

